{
  "name": "fastest-dude-alive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --pretty false",
    "audit": "npm audit --audit-level=high",
    "preview": "vite preview",
    "test:graphics": "node --import ./tests/register.mjs --test tests/*.test.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "^9.16.2",
    "@babylonjs/havok": "^1.3.13"
  },
  "devDependencies": {
    "@napi-rs/canvas": "1.0.8",
    "typescript": "^6.0.2",
    "vite": "^8.1.0"
  },
  "engines": {
    "node": ">=20.19"
  }
}
